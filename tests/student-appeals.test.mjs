import test from "node:test";import assert from "node:assert/strict";import{readFileSync}from"node:fs";
const sql=readFileSync("supabase/STUDENT_APPEALS_V1.sql","utf8"),dashboard=readFileSync("src/components/student/StudentDashboard.tsx","utf8"),api=readFileSync("src/app/api/student/appeals/route.ts","utf8"),admin=readFileSync("src/components/admin/StudentAppealsAdmin.tsx","utf8");
test("student appeals are private and separated from anonymous surveys",()=>{for(const table of ["student_appeals","student_appeal_messages","student_appeal_files","student_appeal_history","student_notifications","student_announcements"])assert.match(sql,new RegExp(`alter table public\\.${table} enable row level security`));assert.match(sql,/revoke all on function public\.student_appeal_context\(text\) from public,anon,authenticated/);assert.doesNotMatch(sql,/student_survey_responses.*student_appeals|student_appeals.*student_survey_responses/)});
test("student API validates the portal session and file limits",()=>{assert.match(api,/getStudentContext/);assert.match(api,/10485760/);assert.match(api,/files\.slice\(0,5\)/)});
test("appeals and notifications are available in all three languages",()=>{for(const source of [dashboard,admin])for(const locale of ["ru","uz","en"])assert.match(source,new RegExp(`${locale}:\\s*\\{`));assert.match(dashboard,/writeAppeal/);assert.match(dashboard,/notifications/)});
